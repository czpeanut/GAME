// Internal render resolution. Wider and shorter than a typical phone/tablet
// screen so the canvas is scaled to fit either orientation; portraits and the
// dialogue box are laid out against this fixed size the same way the rest of
// the app never worries about the real window size.
export const VIEW = { width: 960, height: 540 };

export const FONT = '18px "PingFang TC","Microsoft JhengHei","Noto Sans TC",sans-serif';
export const NAME_FONT = '16px "PingFang TC","Microsoft JhengHei","Noto Sans TC",sans-serif';
